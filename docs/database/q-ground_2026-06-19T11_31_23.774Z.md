erDiagram
	quizzes }o--|| users : references
	comments }o--|| users : references
	comments }o--|| quizzes : references
	leaderboards }o--|| users : references
	leaderboards }o--|| quizzes : references
	quiz_questions }o--|| quizzes : references
	quiz_user_answers ||--|| quiz_questions : references
	quiz_user_answers }o--|| users : references
	quiz_user_answers }o--|| leaderboards : references
	quiz_answers }o--|| quiz_questions : references
	logs }o--|| users : references

	users {
		INTEGER id
		VARCHAR(20) username
		VARCHAR(50) name
		TEXT email
		TIMESTAMP email_verified_at
		VARCHAR(255) password
		ROLE_ENUM role
		TEXT two_factor_secret
		TEXT two_factor_recovery_codes
		TIMESTAMP two_factor_confirmed_at
		VARCHAR(100) remember_token
		TIMESTAMP created_at
		TIMESTAMP updated_at
	}

	quizzes {
		UUID id
		INTEGER user_id
		VARCHAR(100) name
		TEXT description
		NUMERIC(4,3) average_rating
		INTEGER rating_sum
		INTEGER rating_count
		VARCHAR(255) category
		VISIBILITY_ENUM visibility
		QUIZ_TYPE_ENUM quiz_type
		BOOLEAN randomize_question
		BOOLEAN randomize_answer
		TEXT leason material
		BOOLEAN ai_learning_feature
		INTEGER comment_count
		TIMESTAMP created_at
		TIMESTAMP updated_at
	}

	comments {
		INTEGER id
		INTEGER user_id
		UUID quiz_id
		INTEGER leaderboard_id
		TEXT comment
		TIMESTAMP created_at
		TIMESTAMP updated_at
	}

	leaderboards {
		INTEGER id
		INTEGER user_id
		UUID quiz_id
		INTEGER score
		INTEGER attempt_duration_seconds
		INTEGER attempt_number
		SMALLINT leave_count
		TIMESTAMP created_at
		TIMESTAMP updated_at
	}

	quiz_questions {
		INTEGER id
		UUID quiz_id
		TEXT question
		TEXT question_image
		TIMESTAMP created_at
		TIMESTAMP updated_at
	}

	quiz_answers {
		INTEGER id
		INTEGER quiz_question_id
		TEXT answer
		TEXT answer_image
		BOOLEAN is_correct
		TIMESTAMP created_at
		TIMESTAMP updated_at
	}

	quiz_user_answers {
		INTEGER id
		INTEGER user_id
		INTEGER quiz_question_answer_id
		INTEGER leaderboard_id
		INTEGER answer
		INTEGER leave_count
		INTEGER attempt_duration_seconds
		TIMESTAMP created_at
	}

	logs {
		INTEGER id
		INTEGER user_id
		VARCHAR(40) affected_id
		VARCHAR(255) related_table
		VARCHAR(255) action
		TEXT description
		TIMESTAMP created_at
	}

	cache {
		VARCHAR(255) key
		TEXT value
		BIGINT expiration
	}

	cache_locks {
		VARCHAR(255) key
		VARCHAR(255) owner
		BIGINT expiration
	}

	failed_jobs {
		BIGINT id
		VARCHAR(255) uuid
		VARCHAR(255) connection
		VARCHAR(255) queue
		TEXT payload
		TEXT exception
		TIMESTAMP failed_at
	}

	jobs {
		BIGINT id
		VARCHAR(255) queue
		TEXT payload
		SMALLINT attempts
		INTEGER reserved_at
		INTEGER available_at
		INTEGER created_at
	}

	job_batches {
		VARCHAR(255) id
		VARCHAR(255) name
		INTEGER total_jobs
		INTEGER pending_jobs
		INTEGER failed_jobs
		TEXT failed_job_ids
		TEXT options
		INTEGER cancelled_at
		INTEGER created_at
		INTEGER finished_at
	}

	migrations {
		INTEGER id
		VARCHAR(255) migration
		INTEGER batch
	}

	password_reset_tokens {
		VARCHAR(255) email
		VARCHAR(255) token
		TIMESTAMP created_at
	}

	sessions {
		VARCHAR(255) id
		BIGINT user_id
		VARCHAR(45) ip_address
		TEXT user_agent
		TEXT payload
		INTEGER last_activity
	}